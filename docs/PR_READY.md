# PR readiness

The branch now contains the version bump, platform credential boundary, protected-session coordinator, lifecycle relock observer, regression coverage, security contracts, and release-gate documentation. Native credential implementation and final calculator UI integration remain required before merge and release; they stay in this same branch/PR.
