# Storage security scope

Future protected media/storage work remains deferred until the protected-entry release is complete. When resumed, metadata, thumbnails, caches, temporary files, search indexes, and backups must be treated as sensitive. Authenticated chunked decryption and transactional verify-before-commit remain required. Source deletion is optional and permission-gated; no secure-deletion claim is made for flash storage.
