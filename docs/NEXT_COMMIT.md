# Remaining implementation

The next commits on this branch must implement the native credential-verifier channel on Android and iOS and wire the calculator stateful screen to the coordinator, enrollment flow, protected destination, and lifecycle observer. The pull request must remain unmerged until those commits and exact-head CI are green.
