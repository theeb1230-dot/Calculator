# Pull request scope

This branch is limited to the release-worthy protected-entry wave: credential enrollment boundary, authentication-first equals routing, protected-session destination and lifecycle relock, regression tests, documentation, version bump, native credential adapters, UI integration, and release preparation.

Media/files/storage feature expansion is explicitly deferred until the complete 0.3.0 release gate closes.
